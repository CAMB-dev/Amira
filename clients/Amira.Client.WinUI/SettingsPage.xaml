<UserControl
    x:Class="Amira.Client.WinUI.SettingsPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Amira.Client.WinUI"
    AutomationProperties.Name="Settings">
  <UserControl.Resources>
    <ResourceDictionary>
      <local:UserNoticeSeverityConverter x:Key="NoticeSeverity" />
      <local:UserNoticeLiveSettingConverter x:Key="NoticeLiveSetting" />
      <Style x:Key="SectionHeading" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Opacity" Value=".72" />
        <Setter Property="CharacterSpacing" Value="45" />
        <Setter Property="AutomationProperties.HeadingLevel" Value="Level2" />
      </Style>
      <Style x:Key="RowTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
      </Style>
      <Style x:Key="RowDescription" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{ThemeResource MutedText}" />
        <Setter Property="TextWrapping" Value="Wrap" />
      </Style>
      <Style x:Key="SettingsAction" TargetType="Button">
        <Setter Property="MinWidth" Value="92" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="CornerRadius" Value="8" />
      </Style>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid Background="{ThemeResource Side}" PreviewKeyDown="PagePreviewKeyDown">
    <Grid.RowDefinitions>
      <RowDefinition Height="86" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border BorderBrush="{ThemeResource Line}" BorderThickness="0,0,0,1">
      <Grid MaxWidth="860" Padding="30,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center" Spacing="2">
          <TextBlock x:Uid="SettingsPageTitle" Text="Settings" FontSize="28" FontWeight="SemiBold" AutomationProperties.HeadingLevel="Level1" />
          <TextBlock x:Uid="SettingsPageSubtitle" Text="Configure this Windows client." FontSize="13" Foreground="{ThemeResource MutedText}" />
        </StackPanel>
        <Button x:Name="CloseButton"
                x:Uid="SettingsCloseButton"
                Grid.Column="1"
                Width="44"
                Height="44"
                Padding="0"
                Background="Transparent"
                BorderBrush="Transparent"
                Click="CloseClick"
                AutomationProperties.Name="Close settings"
                ToolTipService.ToolTip="Close settings">
          <SymbolIcon Symbol="Cancel" />
        </Button>
      </Grid>
    </Border>

    <ScrollViewer Grid.Row="1"
                  VerticalScrollBarVisibility="Auto"
                  VerticalScrollMode="Enabled"
                  HorizontalScrollMode="Disabled"
                  HorizontalScrollBarVisibility="Disabled"
                  HorizontalContentAlignment="Stretch"
                  ZoomMode="Disabled"
                  AutomationProperties.Name="Settings content">
      <Grid>
      <StackPanel MaxWidth="860" Padding="30,28,30,64" Spacing="34">
        <InfoBar x:Name="SettingsNoticeBar"
                 Message="{Binding Notice.Message}"
                 Severity="{Binding Notice.Severity, Converter={StaticResource NoticeSeverity}}"
                 IsOpen="{Binding HasNotice}"
                 IsClosable="True"
                 Closed="NoticeClosed"
                 AutomationProperties.Name="{Binding Notice.Message}"
                 AutomationProperties.LiveSetting="{Binding Notice.Severity, Converter={StaticResource NoticeLiveSetting}}" />

        <StackPanel Spacing="11">
          <TextBlock x:Uid="SettingsAppearanceSection" Text="APPEARANCE" Style="{StaticResource SectionHeading}" />
          <Border Background="{ThemeResource Surface}" BorderBrush="{ThemeResource Line}" BorderThickness="1" CornerRadius="12">
            <Grid MinHeight="82" Padding="18,13">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel VerticalAlignment="Center" Spacing="3" Margin="0,0,24,0">
                <TextBlock x:Uid="SettingsThemeTitle" Text="App theme" Style="{StaticResource RowTitle}" />
                <TextBlock x:Uid="SettingsThemeDescription" Text="Use the Windows theme or choose an explicit appearance." Style="{StaticResource RowDescription}" />
              </StackPanel>
              <ComboBox x:Name="ThemeSelector"
                        x:Uid="SettingsThemeSelector"
                        Grid.Column="1"
                        Width="184"
                        Height="40"
                        VerticalAlignment="Center"
                        SelectionChanged="ThemeSelectionChanged"
                        AutomationProperties.Name="App theme">
                <ComboBoxItem x:Uid="SettingsThemeSystemOption" Content="Use system setting" />
                <ComboBoxItem x:Uid="SettingsThemeLightOption" Content="Light" />
                <ComboBoxItem x:Uid="SettingsThemeDarkOption" Content="Dark" />
              </ComboBox>
            </Grid>
          </Border>
          <TextBlock x:Uid="SettingsThemeFootnote" Text="Theme changes apply to the whole window and are saved automatically." Style="{StaticResource RowDescription}" Margin="4,0,0,0" />
        </StackPanel>

        <StackPanel Spacing="11">
          <TextBlock x:Uid="SettingsWorkspaceSection" Text="WORKSPACE &amp; DIAGNOSTICS" Style="{StaticResource SectionHeading}" />
          <Border Background="{ThemeResource Surface}" BorderBrush="{ThemeResource Line}" BorderThickness="1" CornerRadius="12">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid MinHeight="82" Padding="18,13">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Spacing="3" Margin="0,0,24,0">
                  <TextBlock x:Uid="SettingsWorkspaceIdTitle" Text="Workspace ID" Style="{StaticResource RowTitle}" />
                  <TextBlock Text="{Binding Environment.WorkspaceId}" FontFamily="Consolas" FontSize="13" Foreground="{ThemeResource MutedText}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Environment.WorkspaceId}" />
                </StackPanel>
                <Button x:Uid="SettingsCopyWorkspaceButton" Grid.Column="1" Content="Copy" Style="{StaticResource SettingsAction}" Click="CopyWorkspaceClick" AutomationProperties.Name="Copy workspace ID" />
              </Grid>

              <Border Grid.Row="1" Height="1" Background="{ThemeResource Line}" Margin="18,0" />

              <Grid Grid.Row="2" MinHeight="82" Padding="18,13">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Spacing="3" Margin="0,0,24,0">
                  <TextBlock x:Uid="SettingsDataFolderTitle" Text="Local data" Style="{StaticResource RowTitle}" />
                  <TextBlock Text="{Binding Environment.DataDirectory}" Style="{StaticResource RowDescription}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" ToolTipService.ToolTip="{Binding Environment.DataDirectory}" />
                </StackPanel>
                <Button x:Uid="SettingsOpenDataButton" Grid.Column="1" Content="Open folder" Style="{StaticResource SettingsAction}" Click="OpenDataClick" AutomationProperties.Name="Open data folder" />
              </Grid>

              <Border Grid.Row="3" Height="1" Background="{ThemeResource Line}" Margin="18,0" />

              <Grid Grid.Row="4" MinHeight="82" Padding="18,13">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Spacing="3" Margin="0,0,24,0">
                  <TextBlock x:Uid="SettingsLogsFolderTitle" Text="Structured logs" Style="{StaticResource RowTitle}" />
                  <TextBlock Text="{Binding Environment.LogsDirectory}" Style="{StaticResource RowDescription}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" ToolTipService.ToolTip="{Binding Environment.LogsDirectory}" />
                </StackPanel>
                <Button x:Uid="SettingsOpenLogsButton" Grid.Column="1" Content="Open folder" Style="{StaticResource SettingsAction}" Click="OpenLogsClick" AutomationProperties.Name="Open logs folder" />
              </Grid>
            </Grid>
          </Border>
          <TextBlock x:Uid="SettingsLoggingPrivacyNote" Text="Logs contain operational identifiers and timings, never prompts, responses, API keys, or request bodies." Style="{StaticResource RowDescription}" Margin="4,0,0,0" />
        </StackPanel>

        <StackPanel Spacing="11">
          <TextBlock x:Uid="SettingsManagementSection" Text="MANAGEMENT" Style="{StaticResource SectionHeading}" />
          <Border Background="{ThemeResource Surface}" BorderBrush="{ThemeResource Line}" BorderThickness="1" CornerRadius="12">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <Grid MinHeight="82" Padding="18,13">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Spacing="3" Margin="0,0,24,0">
                  <TextBlock x:Uid="SettingsConnectionsTitle" Text="Provider connections" Style="{StaticResource RowTitle}" />
                  <TextBlock x:Uid="SettingsConnectionsDescription" Text="Configure endpoints, protocols, credentials, and default models." Style="{StaticResource RowDescription}" />
                </StackPanel>
                <Button x:Uid="SettingsManageConnectionsButton" Grid.Column="1" Content="Manage" Style="{StaticResource SettingsAction}" Click="ManageConnectionsClick" AutomationProperties.Name="Manage provider connections from settings" />
              </Grid>
              <Border Grid.Row="1" Height="1" Background="{ThemeResource Line}" Margin="18,0" />
              <Grid Grid.Row="2" MinHeight="82" Padding="18,13">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Spacing="3" Margin="0,0,24,0">
                  <TextBlock x:Uid="SettingsBotsTitle" Text="Bots" Style="{StaticResource RowTitle}" />
                  <TextBlock x:Uid="SettingsBotsDescription" Text="Create, edit, archive, and restore Bot identities." Style="{StaticResource RowDescription}" />
                </StackPanel>
                <Button x:Uid="SettingsManageBotsButton" Grid.Column="1" Content="Manage" Style="{StaticResource SettingsAction}" Click="ManageBotsClick" AutomationProperties.Name="Manage Bots from settings" />
              </Grid>
            </Grid>
          </Border>
        </StackPanel>

        <StackPanel Spacing="11">
          <TextBlock x:Uid="SettingsAboutSection" Text="ABOUT" Style="{StaticResource SectionHeading}" />
          <Border Background="{ThemeResource Surface}" BorderBrush="{ThemeResource Line}" BorderThickness="1" CornerRadius="12">
            <Grid Padding="18,16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="5*" />
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <TextBlock x:Uid="SettingsVersionLabel" Text="Version" MinHeight="32" Opacity=".66" VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" Text="{Binding Environment.AppVersion}" MinHeight="32" FontWeight="SemiBold" VerticalAlignment="Center" />
              <TextBlock x:Uid="SettingsOperatingSystemLabel" Grid.Row="1" Text="Operating system" MinHeight="32" Opacity=".66" VerticalAlignment="Center" />
              <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Environment.OperatingSystem}" MinHeight="32" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Environment.OperatingSystem}" VerticalAlignment="Center" />
              <TextBlock x:Uid="SettingsRuntimeLabel" Grid.Row="2" Text="Runtime" MinHeight="32" Opacity=".66" VerticalAlignment="Center" />
              <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Environment.Runtime}" MinHeight="32" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Environment.Runtime}" VerticalAlignment="Center" />
              <TextBlock x:Uid="SettingsArchitectureLabel" Grid.Row="3" Text="Architecture" MinHeight="32" Opacity=".66" VerticalAlignment="Center" />
              <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Environment.Architecture}" MinHeight="32" VerticalAlignment="Center" />
            </Grid>
          </Border>
          <Grid Margin="4,0,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Uid="SettingsDiagnosticsDescription" Text="Copy environment details when reporting a problem. Local paths and credentials are excluded." Style="{StaticResource RowDescription}" VerticalAlignment="Center" Margin="0,0,24,0" />
            <Button x:Uid="SettingsCopyDiagnosticsButton" Grid.Column="1" Content="Copy diagnostics" Style="{StaticResource SettingsAction}" Click="CopyDiagnosticsClick" AutomationProperties.Name="Copy diagnostic information" />
          </Grid>
        </StackPanel>
      </StackPanel>
      </Grid>
    </ScrollViewer>
  </Grid>
</UserControl>
